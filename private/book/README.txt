Put the real book PDF here, named:

  from-dating-to-marriage.pdf

(or whatever BOOK_FILENAME is set to in your .env file)

This folder is intentionally outside `public/` so the file can only
be reached through the app's payment-gated API route, never directly.
See the main README.md, section 3, for details.
